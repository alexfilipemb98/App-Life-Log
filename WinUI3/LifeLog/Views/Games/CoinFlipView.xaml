<UserControl
    x:Class="LifeLog.Views.Tools.CoinFlipView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,16" Background="Transparent" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE7FC;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="Games" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <FontIcon Glyph="&#xE76C;" FontFamily="Segoe Fluent Icons" FontSize="10" Foreground="{ThemeResource TextFillColorDisabledBrush}" Margin="4,2,4,0"/>
                
                <Border Background="{ThemeResource SystemAccentColorLight3}" CornerRadius="4" Padding="6,4">
                    <FontIcon Glyph="&#xE819;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource SystemAccentColor}"/>
                </Border>
                <TextBlock Text="Coin Flip" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Padding="0,0,16,0">
            
            <Grid HorizontalAlignment="Center" Margin="24,40,24,64" ColumnSpacing="32">
                
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup>
                        <VisualState x:Name="WideState">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="800"/>
                            </VisualState.StateTriggers>
                        </VisualState>
                        
                        <VisualState x:Name="NarrowState">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0"/>
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Target="CoinSpacer.Visibility" Value="Collapsed"/>
                                <Setter Target="HistoryPanel.(Grid.Column)" Value="0"/>
                                <Setter Target="HistoryPanel.(Grid.Row)" Value="1"/>
                                <Setter Target="HistoryPanel.Margin" Value="0,32,0,0"/>
                                <Setter Target="HistoryPanel.Width" Value="380"/> 
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel x:Name="CoinPanel" Grid.Column="0" Grid.Row="0" Width="380" VerticalAlignment="Top">
                    
                    <Rectangle x:Name="CoinSpacer" Height="52" Fill="Transparent"/>
                    
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="16" Padding="32,48">
                        
                        <StackPanel Spacing="32">
                            <Grid x:Name="CoinWrapper" HorizontalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                                <Grid.RenderTransform>
                                    <ScaleTransform x:Name="CoinScale" ScaleY="1" ScaleX="1"/>
                                </Grid.RenderTransform>
                                
                                <Ellipse Width="170" Height="170" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Opacity="0.5"/>
                                <Ellipse x:Name="CoinBg" Width="150" Height="150" Fill="Goldenrod"/>
                                <FontIcon x:Name="CoinIcon" Glyph="&#xE77B;" FontFamily="Segoe Fluent Icons" Foreground="White" FontSize="72" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>

                            <TextBlock x:Name="CoinText" Text="HEADS" FontSize="18" FontWeight="Black" CharacterSpacing="100" HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

                            <Button x:Name="FlipBtn" Click="FlipBtn_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Height="50" CornerRadius="8">
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <FontIcon Glyph="&#xE819;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                                    <TextBlock Text="FLIP COIN" FontWeight="Bold" CharacterSpacing="50"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel x:Name="HistoryPanel" Grid.Column="1" Grid.Row="0" Width="300" VerticalAlignment="Top">
                    
                    <Grid Height="36" Margin="0,0,0,16">
                        <TextBlock Text="FLIP HISTORY" FontSize="12" FontWeight="Black" Foreground="{ThemeResource TextFillColorSecondaryBrush}" CharacterSpacing="120" VerticalAlignment="Center"/>
                        <Button Click="ClearHistory_Click" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" CornerRadius="6" Height="36" ToolTipService.ToolTip="Clear History" Padding="8,4">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE74D;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                <TextBlock Text="Clear" FontSize="12" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <Border Background="{ThemeResource LayerFillColorAltBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="12" Height="385" Padding="8">
                        
                        <ListView x:Name="HistoryList" SelectionMode="None" IsItemClickEnabled="False">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="8"/>
                                    <Setter Property="MinHeight" Value="0"/>
                                    <Setter Property="Margin" Value="0,0,0,4"/>
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </ListView.ItemContainerStyle>

                            <ListView.ItemContainerTransitions>
                                <TransitionCollection>
                                    <AddDeleteThemeTransition/>
                                </TransitionCollection>
                            </ListView.ItemContainerTransitions>

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Time}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13" VerticalAlignment="Center"/>
                                        <Border Grid.Column="1" Background="{Binding Color}" CornerRadius="6" Padding="16,6" MinWidth="75">
                                            <TextBlock Text="{Binding Result}" Foreground="White" FontWeight="Bold" FontSize="11" HorizontalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>