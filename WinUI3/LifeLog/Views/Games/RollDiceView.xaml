<UserControl
    x:Class="LifeLog.Views.Games.RollDiceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <UserControl.Resources>
        <Storyboard x:Name="RollDiceAnim">
            <DoubleAnimation Storyboard.TargetName="DiceRotate" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:0.7">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut" Exponent="5"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>

            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="DiceScale" Storyboard.TargetProperty="ScaleX">
                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="1.25"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.7" Value="1.0">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <BounceEase EasingMode="EaseOut" Bounces="2" Bounciness="2"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>

            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="DiceScale" Storyboard.TargetProperty="ScaleY">
                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="1.25"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.7" Value="1.0">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <BounceEase EasingMode="EaseOut" Bounces="2" Bounciness="2"/>
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,16" Background="Transparent" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE7FC;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="Games" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <FontIcon Glyph="&#xE76C;" FontFamily="Segoe Fluent Icons" FontSize="10" Foreground="{ThemeResource TextFillColorDisabledBrush}" Margin="4,2,4,0"/>
                
                <Border Background="{ThemeResource SystemAccentColorLight3}" CornerRadius="4" Padding="6,4">
                    <FontIcon Glyph="&#xE8B2;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource SystemAccentColor}"/>
                </Border>
                <TextBlock Text="Roll Dice" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Padding="0,0,16,0">
            
            <Grid HorizontalAlignment="Center" Margin="24,40,24,64" ColumnSpacing="32">
                
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup>
                        <VisualState x:Name="WideState">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="800"/>
                            </VisualState.StateTriggers>
                        </VisualState>
                        <VisualState x:Name="NarrowState">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0"/>
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Target="DiceSpacer.Visibility" Value="Collapsed"/>
                                <Setter Target="HistoryPanel.(Grid.Column)" Value="0"/>
                                <Setter Target="HistoryPanel.(Grid.Row)" Value="1"/>
                                <Setter Target="HistoryPanel.Margin" Value="0,32,0,0"/>
                                <Setter Target="HistoryPanel.Width" Value="380"/> 
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel x:Name="DicePanel" Grid.Column="0" Grid.Row="0" Width="380" VerticalAlignment="Top">
                    <Rectangle x:Name="DiceSpacer" Height="52" Fill="Transparent"/>
                    
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="16" Padding="32,48">
                        
                        <StackPanel Spacing="32">
                            
                            <Border x:Name="DiceBorder" Width="140" Height="140" 
                                    Background="{ThemeResource SystemControlBackgroundAccentBrush}" 
                                    CornerRadius="24" HorizontalAlignment="Center"
                                    RenderTransformOrigin="0.5,0.5">
                                
                                <Border.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform x:Name="DiceScale" ScaleX="1" ScaleY="1"/>
                                        <RotateTransform x:Name="DiceRotate" Angle="0"/>
                                    </TransformGroup>
                                </Border.RenderTransform>
                                
                                <TextBlock x:Name="DiceValueText" Text="?" Foreground="White" 
                                           FontSize="72" FontWeight="Bold" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Margin="0,0,0,8"/>
                            </Border>

                            <TextBlock Text="ROLL TO PLAY" FontSize="16" FontWeight="Black" CharacterSpacing="100" HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                            <Button x:Name="RollBtn" Click="RollBtn_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Height="50" CornerRadius="8">
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <FontIcon Glyph="&#xE8B2;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                                    <TextBlock Text="ROLL DICE" FontWeight="Bold" CharacterSpacing="50"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel x:Name="HistoryPanel" Grid.Column="1" Grid.Row="0" Width="300" VerticalAlignment="Top">
                    <Grid Height="36" Margin="0,0,0,16">
                        <TextBlock Text="LATEST ROLLS" FontSize="12" FontWeight="Black" Foreground="{ThemeResource TextFillColorSecondaryBrush}" CharacterSpacing="120" VerticalAlignment="Center"/>
                        <Button Click="ClearHistoryBtn_Click" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" CornerRadius="6" Height="36" ToolTipService.ToolTip="Clear History" Padding="8,4">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE74D;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                <TextBlock Text="Clear" FontSize="12" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <Border Background="{ThemeResource LayerFillColorAltBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="12" Height="375" Padding="8">
                        
                        <ListView x:Name="HistoryList" SelectionMode="None" IsItemClickEnabled="False">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Padding" Value="8"/>
                                    <Setter Property="MinHeight" Value="0"/>
                                    <Setter Property="Margin" Value="0,0,0,4"/>
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </ListView.ItemContainerStyle>

                            <ListView.ItemContainerTransitions>
                                <TransitionCollection>
                                    <AddDeleteThemeTransition/>
                                </TransitionCollection>
                            </ListView.ItemContainerTransitions>

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Time}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                            <TextBlock Text="Rolled" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                            <Border Background="{ThemeResource SystemAccentColor}" CornerRadius="6" Padding="16,6" MinWidth="40">
                                                <TextBlock Text="{Binding Value}" Foreground="White" FontWeight="Bold" FontSize="13" HorizontalAlignment="Center"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>