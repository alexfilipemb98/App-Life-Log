<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LifeLog.Views.Tools.QrCodeGenView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,16" Background="Transparent" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE74C;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="Tools" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <FontIcon Glyph="&#xE76C;" FontFamily="Segoe Fluent Icons" FontSize="10" Foreground="{ThemeResource TextFillColorDisabledBrush}" Margin="4,2,4,0"/>

                <Border Background="{ThemeResource SystemAccentColorLight3}" CornerRadius="4" Padding="6,4">
                    <FontIcon Glyph="&#xE943;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource SystemAccentColor}"/>
                </Border>
                <TextBlock Text="Code Lab" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Padding="0,0,16,0">
            <Grid MaxWidth="1100" HorizontalAlignment="Center" ColumnSpacing="40" Margin="24,40,24,64">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="420"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="24">
                    <StackPanel.ChildrenTransitions>
                        <TransitionCollection>
                            <EntranceThemeTransition FromVerticalOffset="30" IsStaggeringEnabled="True"/>
                        </TransitionCollection>
                    </StackPanel.ChildrenTransitions>

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="16" Padding="24">
                        <StackPanel Spacing="16">
                            <TextBlock Text="Generator Type" FontWeight="SemiBold" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <RadioButtons x:Name="TypeSelector" SelectionChanged="TypeSelector_SelectionChanged" SelectedIndex="0">
                                <RadioButton Content="Web Link (URL)" Tag="Link" IsChecked="True"/>
                                <RadioButton Content="Plain Text" Tag="Text"/>
                                <RadioButton Content="Wi-Fi Hotspot" Tag="WiFi"/>
                                <RadioButton Content="Barcode (EAN)" Tag="Barcode"/>
                            </RadioButtons>
                        </StackPanel>
                    </Border>

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="16" Padding="24">
                        <Grid>
                            <StackPanel x:Name="PanelLink" Spacing="16">
                                <TextBox x:Name="InputLink" Header="URL" PlaceholderText="https://www.exemplo.com" InputScope="Url" CornerRadius="8"/>
                            </StackPanel>

                            <StackPanel x:Name="PanelText" Spacing="16" Visibility="Collapsed">
                                <TextBox x:Name="InputText" Header="Text Content" PlaceholderText="Escreve aqui a tua mensagem..." AcceptsReturn="True" Height="140" TextWrapping="Wrap" CornerRadius="8"/>
                            </StackPanel>

                            <StackPanel x:Name="PanelWiFi" Spacing="16" Visibility="Collapsed">
                                <TextBox x:Name="WifiSSID" Header="SSID (Nome da Rede)" CornerRadius="8" PlaceholderText="MinhaRede5G"/>
                                <PasswordBox x:Name="WifiPass" Header="Password" CornerRadius="8" PasswordRevealMode="Visible"/>
                                <ComboBox x:Name="WifiEncryption" Header="Security" SelectedIndex="0" HorizontalAlignment="Stretch" CornerRadius="8">
                                    <ComboBoxItem Content="WPA/WPA2"/>
                                    <ComboBoxItem Content="WEP"/>
                                    <ComboBoxItem Content="Open"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel x:Name="PanelBarcode" Spacing="16" Visibility="Collapsed">
                                <TextBox x:Name="BarcodeValue" Header="Barcode Data (EAN/UPC)" PlaceholderText="123456789" CornerRadius="8"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Button x:Name="GenerateBtn" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Height="44" CornerRadius="8" Click="GenerateBtn_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE943;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                            <TextBlock Text="Forge Code" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="24">
                    <StackPanel.ChildrenTransitions>
                        <TransitionCollection>
                            <EntranceThemeTransition FromVerticalOffset="40" IsStaggeringEnabled="True"/>
                        </TransitionCollection>
                    </StackPanel.ChildrenTransitions>

                    <Border Background="{ThemeResource LayerFillColorAltBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                            BorderThickness="1" CornerRadius="16" MinHeight="420" Padding="40">
                        <Grid>
                            <Viewbox MaxHeight="300" MaxWidth="300" VerticalAlignment="Center">
                                <Grid>
                                    <FontIcon x:Name="PlaceholderIcon" Glyph="&#xE943;" FontFamily="Segoe Fluent Icons" FontSize="80" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>

                                    <Border Background="White" CornerRadius="8" Padding="16" Visibility="Collapsed" x:Name="ImageContainer">
                                        <Image x:Name="CodeImage" Stretch="Uniform" Width="250" Height="250"/>
                                    </Border>
                                </Grid>
                            </Viewbox>

                            <TextBlock Text="LIVE PREVIEW" VerticalAlignment="Bottom" HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorTertiaryBrush}" FontSize="11" FontWeight="Black" CharacterSpacing="150"/>
                        </Grid>
                    </Border>

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="CopyBtn" Grid.Column="0" HorizontalAlignment="Stretch" Height="44" CornerRadius="8" IsEnabled="False" Click="CopyBtn_Click" Background="{ThemeResource ControlFillColorDefaultBrush}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8C8;" FontFamily="Segoe Fluent Icons" FontSize="14"/>
                                <TextBlock Text="Copy image" FontWeight="Medium"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="SaveBtn" Grid.Column="1" HorizontalAlignment="Stretch" Height="44" CornerRadius="8" IsEnabled="False" Click="SaveBtn_Click" Background="{ThemeResource ControlFillColorDefaultBrush}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE74E;" FontFamily="Segoe Fluent Icons" FontSize="14"/>
                                <TextBlock Text="Save PNG" FontWeight="Medium"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>