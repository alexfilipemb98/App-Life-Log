<UserControl
    x:Class="LifeLog.Views.Tools.PdfMergeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,16" Background="Transparent" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE74C;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="Tools" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <FontIcon Glyph="&#xE76C;" FontFamily="Segoe Fluent Icons" FontSize="10" Foreground="{ThemeResource TextFillColorDisabledBrush}" Margin="4,2,4,0"/>

                <Border Background="{ThemeResource SystemAccentColorLight3}" CornerRadius="4" Padding="6,4">
                    <FontIcon Glyph="&#xEA90;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource SystemAccentColor}"/>
                </Border>
                <TextBlock Text="PDF Merger" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Padding="0,0,16,0">

            <StackPanel MaxWidth="750" HorizontalAlignment="Center" VerticalAlignment="Top" Spacing="24" Margin="24,40,24,64">

                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromVerticalOffset="30" IsStaggeringEnabled="True"/>
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>

                <InfoBar x:Name="AlertBar" IsOpen="False" IsClosable="True" CornerRadius="8" Margin="0,0,0,8"/>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" 
                        CornerRadius="16" 
                        Padding="40">

                    <StackPanel Spacing="32">

                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                <Border Width="24" Height="24" CornerRadius="12" Background="{ThemeResource SystemFillColorNeutralBrush}">
                                    <TextBlock Text="1" Foreground="{ThemeResource TextFillColorInverseBrush}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Select PDFs Folder" FontSize="15" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="SourceFolderBox" Grid.Column="0" PlaceholderText="Browse for the folder containing your PDF files..." CornerRadius="8" Height="40" VerticalContentAlignment="Center"/>
                                <Button x:Name="BrowseSourceBtn" Grid.Column="1" Click="BrowseSourceBtn_Click" Height="40" Width="44" CornerRadius="8" ToolTipService.ToolTip="Browse Folder" Background="{ThemeResource ControlFillColorDefaultBrush}">
                                    <FontIcon Glyph="&#xE8D5;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                                </Button>
                            </Grid>

                            <Grid ColumnDefinitions="*, Auto" VerticalAlignment="Center">
                                <TextBlock Grid.Column="0" 
               Text="Include subfolders" 
               VerticalAlignment="Center"
               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <ToggleSwitch Grid.Column="1" 
                  x:Name="ToggleSubfolders" 
                  IsOn="False" 
                  OffContent="" 
                  OnContent="" 
                  MinWidth="0"
                  Margin="10,0,0,0"/>
                            </Grid>
                        </StackPanel>

                        <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="-40,0,-40,0"/>

                        <StackPanel Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                                <Border Width="24" Height="24" CornerRadius="12" Background="{ThemeResource SystemFillColorNeutralBrush}">
                                    <TextBlock Text="2" Foreground="{ThemeResource TextFillColorInverseBrush}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Output Destination" FontSize="15" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="OutputFileBox" Grid.Column="0" PlaceholderText="Select where to save the merged PDF..." CornerRadius="8" Height="40" VerticalContentAlignment="Center"/>
                                <Button x:Name="BrowseOutputBtn" Grid.Column="1" Click="BrowseOutputBtn_Click" Height="40" Width="44" CornerRadius="8" ToolTipService.ToolTip="Save As..." Background="{ThemeResource ControlFillColorDefaultBrush}">
                                    <FontIcon Glyph="&#xE792;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                                </Button>
                            </Grid>
                        </StackPanel>

                    </StackPanel>
                </Border>

                <StackPanel Spacing="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="MergeBtn" Grid.Column="0" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Height="44" CornerRadius="8" Click="MergeBtn_Click" Margin="0,0,16,0">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xEA90;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                                <Grid>
                                    <TextBlock x:Name="MergePDfText" Text="Merge PDFs" FontWeight="SemiBold"/>
                                    <ProgressRing x:Name="MergePDfSpinner" Visibility="Collapsed" IsActive="False" Width="24" Height="24" Foreground="{ThemeResource TextFillColorInverseBrush}"/>
                                </Grid>
                            </StackPanel>
                        </Button>

                        <Button x:Name="OpenOutputBtn" Grid.Column="1" Height="44" Padding="16,0" CornerRadius="8" Click="OpenOutputBtn_Click" IsEnabled="False" ToolTipService.ToolTip="Open Output Folder">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8DA;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="Open File" FontWeight="Medium"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <Border CornerRadius="2">
                        <ProgressBar x:Name="MergeProgress" IsIndeterminate="True" Visibility="Collapsed" Height="4"/>
                    </Border>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>