<UserControl
    x:Class="LifeLog.Views.Tools.PassGeneratorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,16" Background="Transparent" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE74C;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock Text="Tools" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <FontIcon Glyph="&#xE76C;" FontFamily="Segoe Fluent Icons" FontSize="10" Foreground="{ThemeResource TextFillColorDisabledBrush}" Margin="4,2,4,0"/>

                <Border Background="{ThemeResource SystemAccentColorLight3}" CornerRadius="4" Padding="6,4">
                    <FontIcon Glyph="&#xE8D7;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource SystemAccentColor}"/>
                </Border>
                <TextBlock Text="Password Generator" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Padding="0,0,16,0">
            <StackPanel MaxWidth="850" HorizontalAlignment="Center" VerticalAlignment="Top" Spacing="32" Margin="24,32,24,64">

                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromVerticalOffset="30" IsStaggeringEnabled="True"/>
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" 
                        CornerRadius="16" 
                        Padding="32">

                    <Grid ColumnSpacing="40">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="24">
                            <TextBlock Text="Complexity" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="14"/>

                            <StackPanel Spacing="4">
                                <Grid>
                                    <TextBlock Text="Password Length" HorizontalAlignment="Left" FontWeight="Medium"/>
                                    <TextBlock x:Name="LengthLabel" Text="20" HorizontalAlignment="Right" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}"/>
                                </Grid>
                                <Slider x:Name="LengthSlider" Minimum="4" Maximum="128" Value="20" ValueChanged="LengthSlider_ValueChanged" Margin="0,8,0,0"/>
                            </StackPanel>

                            <StackPanel Spacing="4">
                                <Grid>
                                    <TextBlock Text="Number of Passwords" HorizontalAlignment="Left" FontWeight="Medium"/>
                                    <TextBlock x:Name="CountLabel" Text="1" HorizontalAlignment="Right" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColor}"/>
                                </Grid>
                                <Slider x:Name="CountSlider" Minimum="1" Maximum="50" Value="1" ValueChanged="CountSlider_ValueChanged" Margin="0,8,0,0"/>
                            </StackPanel>

                            <Rectangle Height="16"/>

                            <Button x:Name="GenerateBtn" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Height="44" CornerRadius="8" Click="GenerateBtn_Click">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE943;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                                    <TextBlock Text="Generate Passwords" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Spacing="16">
                            <TextBlock Text="Character Types" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="14" Margin="0,0,0,8"/>

                            <Grid RowSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Uppercase (A-Z)" VerticalAlignment="Center"/>
                                <ToggleSwitch x:Name="ToggleUpperCase" Grid.Row="0" Grid.Column="1" IsOn="True" OffContent="" OnContent="" Margin="0" MinWidth="0"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Lowercase (a-z)" VerticalAlignment="Center"/>
                                <ToggleSwitch x:Name="ToggleLowerCase" Grid.Row="1" Grid.Column="1" IsOn="True" OffContent="" OnContent="" Margin="0" MinWidth="0"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Numbers (0-9)" VerticalAlignment="Center"/>
                                <ToggleSwitch x:Name="ToggleNumbers" Grid.Row="2" Grid.Column="1" IsOn="True" OffContent="" OnContent="" Margin="0" MinWidth="0"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Symbols (!@#$)" VerticalAlignment="Center"/>
                                <ToggleSwitch x:Name="ToggleSpecialChars" Grid.Row="3" Grid.Column="1" IsOn="True" OffContent="" OnContent="" Margin="0" MinWidth="0"/>

                                <StackPanel Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Spacing="2">
                                    <TextBlock Text="Exclude Ambiguous" VerticalAlignment="Center"/>
                                    <TextBlock Text="(i, l, 1, L, o, O, 0)" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                                <ToggleSwitch x:Name="ToggleExcludeAmbiguous" Grid.Row="4" Grid.Column="1" IsOn="False" OffContent="" OnContent="" Margin="0" MinWidth="0"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <StackPanel Spacing="12">
                    <Grid>
                        <TextBlock Text="Generated Output" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="16" VerticalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                            <Button x:Name="ClearBtn" Background="Transparent" BorderThickness="0" CornerRadius="6" Click="ClearBtn_Click" ToolTipService.ToolTip="Clear output" Height="36">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE74D;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                    <TextBlock Text="Clear" Foreground="{ThemeResource SystemFillColorCriticalBrush}" FontSize="13"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="CopyBtn" Style="{StaticResource AccentButtonStyle}" CornerRadius="6" Click="CopyBtn_Click" Height="36" Padding="16,0">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE8C8;" FontFamily="Segoe Fluent Icons" FontSize="14"/>
                                    <TextBlock Text="Copy all" FontWeight="Medium" FontSize="13"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <ListView x:Name="PasswordsListView" 
                              SelectionMode="None" 
                              IsItemClickEnabled="False"
                              MinHeight="250"
                              Background="{ThemeResource LayerFillColorAltBrush}"
                              CornerRadius="12"
                              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                              BorderThickness="1"
                              Padding="12">

                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,12"/>
                            </Style>
                        </ListView.ItemContainerStyle>

                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding IndexStr}" 
                                               Foreground="{ThemeResource SystemAccentColor}" 
                                               FontWeight="Bold" 
                                               FontSize="16" 
                                               Margin="0,2,0,0"/>

                                    <TextBlock Grid.Column="1" 
                                               Text="{Binding Password}" 
                                               TextWrapping="Wrap" 
                                               FontFamily="Consolas" 
                                               FontSize="16" 
                                               Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                                               IsTextSelectionEnabled="True"/>

                                    <Button Grid.Column="2" 
                                            Click="CopySingleBtn_Click" 
                                            Tag="{Binding Password}" 
                                            VerticalAlignment="Top" 
                                            Background="Transparent" 
                                            BorderThickness="0" 
                                            CornerRadius="6"
                                            Width="36" Height="36"
                                            ToolTipService.ToolTip="Copy this password">
                                        <FontIcon Glyph="&#xE8C8;" FontFamily="Segoe Fluent Icons" FontSize="14"/>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>