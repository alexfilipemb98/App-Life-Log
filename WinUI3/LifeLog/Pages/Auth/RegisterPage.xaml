<Page x:Class="LifeLog.Pages.Auth.RegisterPage" 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <ThemeShadow x:Name="SharedShadow" />
        <Storyboard x:Name="CardHoverEnterAnim">
            <DoubleAnimation Storyboard.TargetName="CardScale" Storyboard.TargetProperty="ScaleX" To="1.02" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="CardScale" Storyboard.TargetProperty="ScaleY" To="1.02" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Name="CardHoverExitAnim">
            <DoubleAnimation Storyboard.TargetName="CardScale" Storyboard.TargetProperty="ScaleX" To="1.0" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="CardScale" Storyboard.TargetProperty="ScaleY" To="1.0" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <ExponentialEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Page.Resources>

    <Grid x:Name="RootGrid">
        <!--<Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{ThemeResource SystemAccentColorLight2}" Offset="0.0" />
                <GradientStop Color="{ThemeResource ApplicationPageBackgroundThemeBrush}" Offset="0.5" />
                <GradientStop Color="{ThemeResource SystemAccentColorDark1}" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>-->

        <Border x:Name="RegisterCard"
                HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" 
                Background="{ThemeResource LayerFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" 
                CornerRadius="16" Padding="40"
                Shadow="{StaticResource SharedShadow}" Translation="0,0,40"
                PointerEntered="RegisterCard_PointerEntered" PointerExited="RegisterCard_PointerExited">

            <Border.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="CardScale" CenterX="200" CenterY="270" />
                    <TranslateTransform x:Name="CardShakeTransform" />
                </TransformGroup>
            </Border.RenderTransform>

            <StackPanel Spacing="24">
                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromVerticalOffset="40" IsStaggeringEnabled="True" />
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>

                <StackPanel Spacing="12" HorizontalAlignment="Center">
                    <Border Width="64" Height="64" CornerRadius="20" Background="{ThemeResource SystemAccentColor}" HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE8FA;" FontSize="30" Foreground="White" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Create Account" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" />
                </StackPanel>

                <StackPanel Spacing="16">
                    <TextBox x:Name="UsernameBox" Header="Username" PlaceholderText="your_username" TextChanged="ValidateInputs" KeyDown="Input_KeyDown" CornerRadius="8"/>

                    <TextBox x:Name="EmailBox" Header="Email" PlaceholderText="your@email.com" InputScope="EmailNameOrAddress" TextChanged="ValidateInputs" KeyDown="Input_KeyDown" CornerRadius="8"/>

                    <StackPanel Spacing="4">
                        <PasswordBox x:Name="PassBox" Header="Password" PlaceholderText="????????????????????????" PasswordChanged="ValidateInputs" 
                                     PasswordRevealMode="Visible" GotFocus="PassBox_GotFocus" LostFocus="PassBox_LostFocus" KeyDown="PassBox_KeyDown" CornerRadius="8"/>
                        <TextBlock x:Name="CapsLockWarning" Text="?????? Caps Lock is ON" Foreground="{ThemeResource SystemFillColorCautionBrush}" FontSize="12" Visibility="Collapsed" Margin="2,0,0,0" FontWeight="Medium"/>
                    </StackPanel>

                    <TextBlock x:Name="ErrorMessage" Text="Invalid input." Foreground="{ThemeResource SystemFillColorCriticalBrush}" FontSize="12" Visibility="Collapsed" Margin="2,0,0,0" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Spacing="16">
                    <Button x:Name="SignUpButton" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}" Height="44" CornerRadius="8" Click="SignUp_Click">
                        <Grid>
                            <TextBlock x:Name="SignUpText" Text="Sign Up" FontWeight="SemiBold"/>
                            <ProgressRing x:Name="SignUpSpinner" Visibility="Collapsed" IsActive="False" Width="24" Height="24" Foreground="{ThemeResource TextFillColorInverseBrush}"/>
                        </Grid>
                    </Button>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="4">
                        <TextBlock Text="Already have an account?" Opacity="0.7" VerticalAlignment="Center"/>
                        <HyperlinkButton Content="Sign in" FontSize="14" Padding="4,0" Click="GoToLogin_Click"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>